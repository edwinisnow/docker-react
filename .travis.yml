language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t edwinisnow/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true edwinisnow/docker-react npm run test
